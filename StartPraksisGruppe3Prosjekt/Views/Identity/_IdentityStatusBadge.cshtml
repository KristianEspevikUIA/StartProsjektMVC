@model IdentityStatus
@* The status in words, on the status colour. Never the colour alone. *@
<span class="sc-id-status sc-id-status--@IdentityStatuses.CssKey(Model)">@IdentityStatuses.Label(Model)</span>
