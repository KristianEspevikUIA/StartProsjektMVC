@model PlayerDetailViewModel
@{
    ViewData["Title"] = "My answers";
}

@*
    Superseded. PlayerController.MyResponses now redirects to Index, which renders the
    shared 5C page. Kept so an old bookmark resolves to something rather than a 500.
*@

<h1>My answers</h1>
<p class="text-muted">@Model.TeamName</p>
<p><a asp-action="Index">Go to my 5C page</a></p>
