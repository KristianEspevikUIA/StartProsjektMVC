@{
    ViewData["Title"] = "Consent";
}

<h1>Consent</h1>

<p class="text-muted">
    Consent is an append-only log: withdrawing it adds a new event at a lower level rather
    than editing the old one, so the club can always document what was allowed when.
</p>

@* TODO (Brage): build ConsentViewModel from GetCurrentLevelAsync + GetHistoryAsync, and
   post changes through IConsentService.RecordAsync. Never edit or delete a row —
   AppDbContext throws if you try. *@
<p><em>Not built yet.</em></p>
