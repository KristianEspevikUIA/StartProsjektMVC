{
  // Utvikling kjører på localhost, en LAN-adresse eller en tunnel, og bytter ofte.
  // Vertsnavnfilteret hører hjemme i produksjon — se appsettings.json.
  "AllowedHosts": "*",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
