@{
    // Identity UI ships with its own layout. Pointing it at ours is what gives the sign-in
    // pages the StartCompass header, footer and palette instead of a bare Bootstrap page
    // with a different name in the title bar.
    Layout = "/Views/Shared/_Layout.cshtml";
}
